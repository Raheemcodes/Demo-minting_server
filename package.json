{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "start": "node dist/app.js",
    "serve": "nodemon dist/app.js",
    "dev": "nodemon src/app.ts",
    "build": "tsc",
    "deploy": "npm run build && 7z a -r rest-api.zip dist node_modules package.json package-lock.json tsconfig.json lambda.js",
    "create": "aws lambda create-function --function-name demo-dapp-server --runtime nodejs16.x --role arn:aws:iam::052146137548:role/lambda-execution-role --zip-file fileb://demo-dapp-server.zip --handler dist/lambda.handler",
    "update": "npm run deploy && aws lambda update-function-code --function-name demo-dapp-server --zip-file fileb://demo-dapp-server.zip",
    "test": "jest --watchAll"
  },
  "author": "Raheem",
  "license": "ISC",
  "dependencies": {
    "@types/aws-lambda": "^8.10.119",
    "compression": "^1.7.4",
    "express": "^4.18.2",
    "express-validator": "^7.0.1",
    "helmet": "^7.0.0",
    "web3": "^4.0.3"
  },
  "devDependencies": {
    "@types/compression": "^1.7.2",
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.3",
    "@types/node": "^20.4.3",
    "mongoose": "^7.4.1",
    "ts-jest": "^29.1.1",
    "ts-node": "^10.9.1"
  }
}
